David J. Malan

Functions & Variables
Conditionals
Loops
Exceptions (catch exeptions)
Libraries
Unit Tests
file I/O
Regular Expressions
Object-Oriented Programming

on VS Code Terminal
code hello.py --> creates a python file
python hello.py --> runs the file

Pseudocode - Structuring code using comments

We can add a default value inside functions 
def hello(name="world"):

terminal commands
ls
cp
mv
rm
mkdir
cd
rmdir
cd
rmdir
clear

to change global variables write
global <variable name>

we can use while loop to run infinately until user gives correct input

We can use _ in a for loop
for _ in range(3):

we can use print to sep and control its end


##Dictionaries

name age gender
harry 20 Male
peter 20 Male
angel 18 FeMale

dict = [
{"name": "harry", "age": 20, "gender": "male"},
{"name": "peter", "age": 20, "gender": "male"},
{"name": "angel", "age": 18, "gender": "female"},
]

name age gender
harry 20 Male
name age gender
peter 20 Male
name age gender
angel 18 FeMale


dict = 
{ "name": "value",
  "name": "value"
}

dict["name"] --> to get value
dict.get("address", NA) --> add a default value if the key is not accesable
for d in dict: --> to get keys
   print(d)
for d in dict.keys(): --> to get keys
   print(d)

.values --> to get values
dict.update({"address": "New York", "Code": "NY"}) --> Add a dictionary to update the dict

### Methods
dictionary 
.pop()
.clear()
.update() - add another dictionary
.keys()
.items()
.values()

Lists
.pop()
.extend() - add another list
.reverese()

import sys
sys.getsizeof()

tuple
list comprehensions
[w for w in words if w>3]

while loops 
use it when you don't know how many times you want the loop to run
